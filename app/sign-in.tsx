import React,{useState}from'react';import{SafeAreaView,View,Text,TextInput,Pressable,StyleSheet,Alert,ScrollView,KeyboardAvoidingView,Platform}from'react-native';import{router}from'expo-router';import{supabase}from'../lib/supabase';import{authRedirectUrl}from'../lib/authLinks';import{C}from'../lib/theme';export default function SignIn(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);async function go(){setBusy(true);const{error}=await supabase.auth.signInWithPassword({email:email.trim(),password});setBusy(false);if(error)return Alert.alert('Could not sign in',error.message);router.replace('/')}async function reset(){if(!email.trim())return Alert.alert('Enter your email first');const{error}=await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:authRedirectUrl});Alert.alert(error?'Could not send email':'Check your email',error?.message??'We sent a password reset link.')}return <SafeAreaView style={s.safe}><KeyboardAvoidingView style={{flex:1}} behavior={Platform.OS==='ios'?'padding':undefined} keyboardVerticalOffset={8}><ScrollView contentContainerStyle={s.page} keyboardShouldPersistTaps="handled" keyboardDismissMode="interactive"><Text style={s.h}>Welcome Back</Text><Text style={s.sub}>Sign in to reconnect with your T1Together community.</Text><TextInput style={s.input} placeholder="Email" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail}/><TextInput style={s.input} placeholder="Password" secureTextEntry value={password} onChangeText={setPassword}/><Pressable style={s.btn} onPress={go} disabled={busy}><Text style={s.bt}>{busy?'Signing in…':'Sign In'}</Text></Pressable><Pressable onPress={reset}><Text style={s.link}>Forgot Password?</Text></Pressable><Pressable onPress={()=>router.push('/sign-up')}><Text style={s.link}>Create an Account</Text></Pressable><View style={{height:24}}/></ScrollView></KeyboardAvoidingView></SafeAreaView>}const s=StyleSheet.create({safe:{flex:1,backgroundColor:C.white},page:{padding:24,paddingTop:70},h:{fontSize:32,fontWeight:'900',color:C.navy},sub:{color:C.gray,fontSize:16,marginTop:5,marginBottom:28},input:{borderWidth:1,borderColor:C.line,borderRadius:14,padding:15,fontSize:16,marginBottom:12},btn:{backgroundColor:C.blue,borderRadius:15,padding:16,alignItems:'center',marginTop:8},bt:{color:C.white,fontWeight:'900',fontSize:16},link:{color:C.blue,fontWeight:'700',textAlign:'center',marginTop:20}});