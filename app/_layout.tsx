import React,{useEffect}from'react';import{Stack,router}from'expo-router';import * as Notifications from'expo-notifications';import{AuthProvider,useAuth}from'../lib/AuthContext';import{registerPush,notificationRoute}from'../lib/notifications';import{refreshApproximateLocationIfStale}from'../lib/location';
function NotificationBridge(){const{session}=useAuth();useEffect(()=>{if(session?.user?.id){registerPush(session.user.id);refreshApproximateLocationIfStale().catch(()=>{})}},[session?.user?.id]);useEffect(()=>{const sub=Notifications.addNotificationResponseReceivedListener(r=>router.push(notificationRoute(r.notification.request.content.data)));Notifications.getLastNotificationResponseAsync().then(r=>{if(r)router.push(notificationRoute(r.notification.request.content.data))});return()=>sub.remove()},[]);return null}
export default function Layout(){return <AuthProvider><NotificationBridge/><Stack screenOptions={{headerShown:false}}/></AuthProvider>}